# AGENTS.md

MyManabi リポジトリで作業する AI エージェント／開発者向けの最小ルール。

## プロジェクト構造

- `docs/concept/` — 目的・方針・前提・到達点・保留事項・リスク
- `docs/spec/` — 学習フロー・理解度測定・教材管理・学習履歴・レポート・データ配置などの具体仕様
- （将来）`src/` — アプリ本体のコード
- （将来）`examples/` — DATA_DIR の合成データ例（個人情報を含まない）

## 安全（最重要）

このリポジトリは Public で公開する。子どもの学習を扱うため、次を厳守する。

- 実際の学習データ（学習者の記録・手書き画像・音声などの **個人情報**）を**絶対にコミットしない**
- 実教科書由来の **教材コンテンツ（著作権）** も**コミットしない**
- 実データ・教材は本リポジトリ外の DATA_DIR（例: `%APPDATA%/MyManabi`）に置く
- リポジトリに入れてよいのは、合成データ（`examples/`）とスキーマのみ

### AI サービスの学習利用設定

本アプリは ChatGPT / Codex など外部 AI サービスを利用する。教材・学習データを AI のトレーニングに使用させないため、**利用者は各サービスで「学習利用オフ」を必ず設定すること**。

- ChatGPT: 設定 → データコントロール → 「全員のためにモデルを改善する」をオフ
- 著作権保護の観点から、この設定は利用開始前に確認する

設計上、教材・学習データはリポジトリ外の DATA_DIR に隔離している（コミットによる漏洩は防止済み）。ただし AI サービスへの送信時の扱いは利用者側の設定に依存するため、各自で対応が必要。

## 文字コード

- すべて UTF-8 で保存する

## リンク規約

- 公開 `docs/` 内のリンクは **標準 markdown リンク** を使う（例: `[spec](../spec/README.md)`）
- Obsidian の `[[wikilink]]` は非公開ヴォルト内のみで使う。公開 `docs/` では使わない

## Mermaid

- Obsidian の Mermaid では、ノードの文字囲み `["..."]` 内でリスト記法（`1. ` や `- ` のように後ろに半角スペースがある形）を使うと `Unsupported markdown: list` エラーになる
- 代替表現（`(1) ` や `① `）を使う

## ドキュメントと進捗の管理

機能の洗い出しと進捗管理においては、情報の一貫性を保つため以下のルールを厳守する。

- `docs/spec/initial-poc-roadmap.md` を進捗トラッキングの唯一の正本（SSOT）とする
- `docs/architecture.md` や `docs/spec/domain-design.md` など、他の設計ドキュメントではステータスや状態表を重複管理しない
- ロードマップの状態値は `未着手` / `進行中` / `完了` / `保留` の明示語に統一する（`[x]` などのチェックボックス記法は検索性が低いため使わない）
- `完了` ステータスは、「確認方法」列が埋まっている（確認済みである）ときだけ設定する
- 検討中に発生した「将来課題」や「アイデア」はロードマップへ直書きせず、別リスト（各仕様書の保留事項など）で管理し、優先度が決まったものだけをロードマップへ移す

※ これらのルールを適用する作業手順は `.agents/skills/manage-roadmap/SKILL.md` に定義されている。進捗を更新する際は、必ずこの Skill を適用すること。
